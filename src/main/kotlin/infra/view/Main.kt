package infra.view

fun main(args: Array<String>) {

    // Main class
    // It should create an accountService, do all the operations needed, and print the statement on console
    // Note that the accounService needs a transactionRepository implementation, so it should also be created here!

}
